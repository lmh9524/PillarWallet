# PillarWallet 中文语言设置相关文件说明

> 📅 生成日期: 2024-11-27  
> 📌 项目路径: `d:\KKK\metamask-v3\pillarwallet-clean`  
> 🔧 i18next 版本: v23.4.5  
> ⚛️ react-i18next 版本: v15.0.6  
> 📱 react-native-localize 版本: v3.5.1

---

## 📑 目录

1. [核心配置文件](#核心配置文件)
2. [翻译资源文件](#翻译资源文件)
3. [语言切换组件](#语言切换组件)
4. [状态管理文件](#状态管理文件)
5. [服务层文件](#服务层文件)
6. [工具函数](#工具函数)
7. [文件关系图](#文件关系图)
8. [快速定位指南](#快速定位指南)

---

## 1️⃣ 核心配置文件

### 1.0 `package.json` - 依赖包版本

**国际化相关依赖**:
```json
{
  "dependencies": {
    "i18next": "^23.4.5",                    // ⚠️ i18next 核心库
    "react-i18next": "^15.0.6",              // ⚠️ React 集成
    "react-native-localize": "^3.5.1",      // ⚠️ 设备语言检测
    "@reduxjs/toolkit": "^2.8.2",           // Redux 状态管理
    "react": "19.1.0",
    "react-native": "0.80.1"
  }
}
```

**关键依赖说明**:
| 包名 | 版本 | 作用 |
|------|------|------|
| `i18next` | v23.4.5 | 国际化核心引擎 |
| `react-i18next` | v15.0.6 | React/RN 集成层 |
| `react-native-localize` | v3.5.1 | 获取设备语言/区域设置 |

**路径**: `package.json`

---

### 1.1 `src/configs/localeConfig.js`

**功能**: 语言配置的核心文件，定义所有支持的语言及其资源

**关键配置**:
```javascript
{
  isEnabled: true,                          // 是否启用国际化
  defaultLanguage: 'en',                    // 默认语言
  supportedLanguages: {
    en: 'English',
    am: 'አማርኛ',
    bs: 'Bosanski',
    cn: 'Chinese (Simplified)',             // ⚠️ 中文语言标识
  },
  defaultNameSpace: 'common',
  namespaces: ['auth', 'common'],           // 翻译命名空间
  localTranslations: {
    en: { common: EN_COMMON, auth: EN_AUTH },
    cn: { common: CN_COMMON, auth: CN_AUTH }, // 中文翻译资源
  }
}
```

**修改建议**:
- ✅ 将 `cn: 'Chinese (Simplified)'` 改为 `cn: '简体中文'` 以改善用户体验
- ⚠️ 考虑使用 ISO 639-1 标准代码 `zh` 代替 `cn`

**路径**: `src/configs/localeConfig.js`

---

### 1.2 `src/constants/localesConstants.js`

**功能**: 定义国际化相关常量

**内容**:
```javascript
export const CAPITALIZATION_POSTPROCESSOR = 'capitalization';
export const PUNCTUATION_POSTPROCESSOR = 'punctuation';
export const SUFFIX_PREFIX_POSTPROCESSOR = 'suffixPrefix';
```

**路径**: `src/constants/localesConstants.js`

---

## 2️⃣ 翻译资源文件

### 2.1 中文翻译文件

#### `src/locales/zh-CN/common.json`

**功能**: 通用中文翻译（1602 行）

**内容结构**:
```json
{
  "aaveDeposit": "Aave 存款",
  "ensName": "ENS名称",
  "ethereum": "Ethereum",
  "tutorialTitle": "欢迎来到 PillarX！",
  
  "chains": {
    "ethereum": "Ethereum",
    "binance": "BSC",
    "polygon": "Polygon"
  },
  
  "button": {
    "activate": "启用",
    "add": "添加",
    "send": "发送"
  },
  
  "error": {
    "somethingWentWrong": "出了点问题"
  }
}
```

**已知问题**:
- ❌ 比英文版少 177 行（~10%）
- ❌ 存在机器翻译质量问题
- ❌ 专有名词翻译不一致

**路径**: `src/locales/zh-CN/common.json`

---

#### `src/locales/zh-CN/auth.json`

**功能**: 认证相关中文翻译

**内容结构**:
```json
{
  "button": {
    "createWallet": "创建钱包",
    "importWallet": "导入钱包",
    "next": "下一步"
  },
  
  "title": {
    "welcomeToPillar": "欢迎来到支柱",
    "createPin": "创建 PIN 码",
    "confirmPin": "确认密码"
  },
  
  "paragraph": {
    "restoreWalletWarning": "没有备份短语是不可能恢复你的钱包的，小心。"
  }
}
```

**路径**: `src/locales/zh-CN/auth.json`

---

### 2.2 英文翻译文件（对照参考）

#### `src/locales/en/common.json` (1779 行)
#### `src/locales/en/auth.json`

**路径**: 
- `src/locales/en/common.json`
- `src/locales/en/auth.json`

---

### 2.3 其他语言文件

- **阿姆哈拉语**: `src/locales/am/`
- **波斯尼亚语**: `src/locales/bs/`

---

## 3️⃣ 语言切换组件

### 3.1 `src/screens/Menu/SelectLanguage.js`

**功能**: 语言选择界面

**关键代码**:
```javascript
const languages = [
  { code: 'en', name: 'English', icon: flagEnIcon },
  { code: 'am', name: 'አማርኛ', icon: flagAmIcon },
  { code: 'bs', name: 'Bosanski', icon: flagBsIcon },
  { code: 'cn', name: '简体中文', icon: flagCnIcon },
];

const handleLanguageSelect = (languageCode) => {
  dispatch(changeLanguageAction(languageCode));
  navigation.goBack();
};
```

**UI 组件**:
- 显示语言列表
- 当前选中语言高亮
- 点击切换语言

**路径**: `src/screens/Menu/SelectLanguage.js`

---

### 3.2 `src/screens/Menu/Settings/components/LanguageSetting.js`

**功能**: 设置页面的语言设置入口

**关键代码**:
```javascript
const LanguageSetting = () => {
  const navigation = useNavigation();
  const { t } = useTranslationWithPrefix('settings');
  const currentLanguage = useRootSelector(languageSelector);
  
  return (
    <SettingsItem
      title={t('language')}
      value={getSupportedLanguages()[currentLanguage]}
      onPress={() => navigation.navigate(MENU_SELECT_LANGUAGE)}
    />
  );
};
```

**功能说明**:
- 显示当前语言
- 点击跳转到语言选择页面
- ⚠️ 此组件在代码中被注释禁用了

**路径**: `src/screens/Menu/Settings/components/LanguageSetting.js`

---

### 3.3 语言图标资源

**中文旗帜图标**: `src/assets/icons/svg/icon-16-language.svg`

**路径**: `src/assets/icons/svg/icon-16-language.svg`

---

## 4️⃣ 状态管理文件

### 4.1 Actions (动作文件)

#### `src/actions/localisationActions.js`

**功能**: 语言切换核心逻辑

**关键类型定义**:
```javascript
type SetLngAndBundle = {
  language: string,
  resources: TranslationResourcesOfLanguage,
  onSuccess?: () => void,
};

type GetTranslationResourcesProps = {
  language: string,
  dispatch: Dispatch,
  getState: GetState,
  getLocal?: boolean,
};
```

**关键函数**:

##### `getTranslationsResources(props)` - 获取翻译资源
```javascript
const getTranslationsResources = async (props: GetTranslationResourcesProps) => {
  const { language, dispatch, getState } = props;
  let resources;
  let version = firebaseRemoteConfig.getString(REMOTE_CONFIG.APP_LOCALES_LATEST_TIMESTAMP);
  const baseUrl = firebaseRemoteConfig.getString(REMOTE_CONFIG.APP_LOCALES_URL);

  // 如果提供了 baseUrl，使用外部翻译；否则使用本地翻译
  if (baseUrl) {
    // 从缓存或网络获取翻译资源
    resources = await getCachedTranslationResources(/* ... */);
  } else {
    // 使用本地打包的翻译
    resources = getLocalTranslations(language);
    version = 'LOCAL';
  }

  return { resources, version, missingNsArray };
};
```

##### `setLanguageAndTranslationBundles({ resources, language, onSuccess })`
```javascript
const setLanguageAndTranslationBundles = async ({ 
  resources, 
  language, 
  onSuccess 
}: SetLngAndBundle) => {
  // 添加翻译资源包
  addResourceBundles(language, localeConfig.namespaces, resources);
  
  // 切换语言
  await setLanguage(language);  // ⚠️ 只传一个参数
  
  if (onSuccess) {
    onSuccess();
  }
};
```

##### `changeLanguageAction(language: string)` ⚠️ 完整实现
```javascript
export const changeLanguageAction = (language: string) => {
  return async (dispatch: Dispatch, getState: GetState) => {
    const {
      session: {
        data: { fallbackLanguageVersion },
      },
    } = getState();

    if (!localeConfig.isEnabled) return;

    if (isLanguageSupported(language)) {
      // 1. 获取翻译资源
      const { resources, version } = await getTranslationsResources({ 
        language, 
        dispatch, 
        getState 
      });
      
      // 2. 定义成功回调
      const onLanguageChangeSuccess = () => {
        dispatch(setAppLanguageAction(language));

        Toast.show({
          message: t('toast.languageChanged'),
          emoji: 'ok_hand',  // ⚠️ 实际使用的 emoji
          autoClose: true,
        });
      };

      // 3. 设置语言和翻译包
      await setLanguageAndTranslationBundles({ 
        resources, 
        language, 
        onSuccess: onLanguageChangeSuccess 
      });

      // 4. 更新回退语言版本
      if (language === localeConfig.defaultLanguage) {
        dispatch(setFallbackLanguageVersionAction(version));
      } else if (!fallbackLanguageVersion || fallbackLanguageVersion === 'LOCAL') {
        await dispatch(getAndSetFallbackLanguageResources());
      }
      
      // 5. 更新 session
      dispatch(setSessionLanguageAction(language, version));
    } else {
      // 不支持的语言
      Toast.show({
        message: t('toast.languageIsNotSupported'),
        emoji: 'hushed',
        autoClose: true,
      });
    }
  };
};
```

**功能说明**:
1. ✅ 检查国际化是否启用
2. ✅ 检查语言是否支持
3. ✅ 获取翻译资源（从缓存/网络/本地）
4. ✅ 添加资源包并切换语言
5. ✅ 更新应用设置（持久化）
6. ✅ 更新会话状态
7. ✅ 处理回退语言
8. ✅ 显示成功/失败提示

**路径**: `src/actions/localisationActions.js`

---

#### `src/actions/appSettingsActions.js`

**功能**: 应用设置相关动作

**关键函数**:

##### `setAppLanguageAction(language: string)`
```javascript
export const setAppLanguageAction = (language: string) => {
  return (dispatch: Dispatch) => {
    const localisationSettings = { 
      localisation: { 
        activeLngCode: language 
      } 
    };
    
    dispatch({
      type: UPDATE_APP_SETTINGS,
      payload: localisationSettings,
    });
    
    dispatch(saveDbAction('appSettings', localisationSettings, true));
  };
};
```

**功能说明**:
1. 更新应用设置中的语言代码
2. 保存到数据库

**路径**: `src/actions/appSettingsActions.js`

---

#### `src/actions/sessionActions.js`

**功能**: 会话相关动作

**关键函数**:

##### `setSessionLanguageAction(language: string, version: string)`
```javascript
export const setSessionLanguageAction = (languageCode: string, version: string) => {
  return (dispatch: Dispatch) => {
    dispatch({
      type: UPDATE_SESSION,
      payload: {
        translationsInitialised: true,
        sessionLanguageCode: languageCode,
        sessionLanguageVersion: version,
      },
    });
  };
};
```

**路径**: `src/actions/sessionActions.js`

---

### 4.2 Reducers (状态管理器)

#### `src/reducers/appSettingsReducer.js`

**功能**: 应用设置状态管理

**状态结构**:
```javascript
type AppSettingsReducerState = {
  data: {
    localisation: ?LocalisationOptions,  // 语言设置
    themeType: string,                   // 主题
    baseFiatCurrency: ?Currency,         // 货币
    // ... 其他设置
  },
  isFetched: boolean,
};

// LocalisationOptions 结构
type LocalisationOptions = {
  activeLngCode: string,  // 当前激活的语言代码，如 'cn'
};
```

**初始状态**:
```javascript
const initialState = {
  data: {
    localisation: null,  // 初始为 null，首次启动时根据设备语言设置
    // ...
  },
  isFetched: false,
};
```

**路径**: `src/reducers/appSettingsReducer.js`

---

#### `src/reducers/sessionReducer.js`

**功能**: 会话状态管理

**状态结构**:
```javascript
type SessionReducerState = {
  data: {
    isOnline: boolean,
    translationsInitialised: boolean,      // 翻译是否已初始化
    sessionLanguageCode: ?string,          // 会话语言代码
    sessionLanguageVersion: ?string,       // 翻译资源版本
    fallbackLanguageVersion: ?string,      // 回退语言版本
  }
};
```

**路径**: `src/reducers/sessionReducer.js`

---

### 4.3 Selectors (选择器)

#### `src/selectors/appSettings.js`

**功能**: 从 Redux store 中选择应用设置

**关键选择器**:
```javascript
export const languageSelector = createSelector(
  localisationSelector,
  (localisation) => localisation?.activeLngCode || 'en'
);
```

**路径**: `src/selectors/appSettings.js`

---

## 5️⃣ 服务层文件

### 5.1 `src/services/localisation/translations.tsx`

**功能**: i18next 初始化和翻译服务核心

**依赖包**:
- `i18next` v23.4.5
- `react-i18next` v15.0.6
- `react-native-localize` v3.5.1 ⚠️ 用于获取设备语言

**关键函数**:

##### `i18n 初始化` (自动执行)
```javascript
import i18n from 'i18next';
import { getLocales } from 'react-native-localize';  // ⚠️ 实际使用
import { initReactI18next } from 'react-i18next';
import { PunctuationPostProcessor, CapitalizationPostProcessor, SuffixPrefixPostProcessor } from './postProcessors';

/* eslint-disable i18next/no-literal-string */
i18n
  .use(initReactI18next)
  .use(PunctuationPostProcessor)        // ⚠️ 注意：大写开头
  .use(SuffixPrefixPostProcessor)
  .use(CapitalizationPostProcessor)
  .init(
    {
      compatibilityJSON: 'v3',
      interpolation: { escapeValue: false },
      ns: localeConfig.namespaces,
      defaultNS: localeConfig.defaultNameSpace,
      postProcess: localeConfig.postProcessors,
      fallbackLng: localeConfig.defaultLanguage,
      supportedLngs: Object.keys(localeConfig.supportedLanguages),
      debug: !!__DEV__,
      react: {
        nsMode: 'default',
        useSuspense: true,
      },
      lng: localeConfig.defaultLanguage,
      resources: {
        [localeConfig.defaultLanguage]: {},
      },
    },
    () => {},
  );
/* eslint-enable i18next/no-literal-string */
```

##### `addResourceBundles(lng, nameSpaces, translations)`
```javascript
export const addResourceBundles = (
  lng: string, 
  nameSpaces: string[], 
  translations: TranslationResourcesOfLanguage
) => {
  nameSpaces.forEach((ns) => {
    if (translations[ns]) {
      i18n.addResourceBundle(lng, ns, translations[ns], true, false);
    }
  });
  i18n.reloadResources();
};
```

##### `setLanguage(lng: string)` ⚠️ 注意：只接受一个参数
```javascript
export const setLanguage = async (lng: string) => {
  i18n
    .changeLanguage(lng)
    .then(() => Promise.resolve())
    .catch(() => Promise.reject());
};
```

##### `getDefaultSupportedUserLanguage()` ⚠️ 使用 react-native-localize
```javascript
import { getLocales } from 'react-native-localize';

export const getDefaultSupportedUserLanguage = () => {  
  const userPreferredLocales = getLocales();
  
  const userPreferredLanguages = userPreferredLocales?.map(
    ({ languageCode }) => languageCode
  );

  const userPreferredSupportedLanguage = userPreferredLanguages?.find(
    (languageCode) => Object.keys(localeConfig.supportedLanguages).includes(languageCode)
  );

  return userPreferredSupportedLanguage || localeConfig.defaultLanguage;
};
```

##### `isLanguageSupported(language: string)`
```javascript
export const isLanguageSupported = (language: string) => {
  return Object.keys(localeConfig.supportedLanguages).includes(language);
};
```

##### `getLanguageFullName(languageCode: string)`
```javascript
export const getLanguageFullName = (languageCode: string) => {
  return localeConfig.supportedLanguages?.[languageCode];
};
```

**路径**: `src/services/localisation/translations.tsx`

---

### 5.2 `src/services/localisation/postProcessors.js`

**功能**: i18next 后处理器，处理文本格式化

**包含处理器**:
- **capitalizationPostProcessor**: 首字母大写
- **punctuationPostProcessor**: 标点符号处理
- **suffixPrefixPostProcessor**: 前后缀处理

**路径**: `src/services/localisation/postProcessors.js`

---

### 5.3 `src/services/localisation/README.md`

**功能**: 国际化服务使用说明文档

**路径**: `src/services/localisation/README.md`

---

## 6️⃣ 工具函数

### 6.1 `src/translations/translate.tsx`

**功能**: 翻译函数封装

**核心导出**:
```javascript
// 默认翻译函数
const translate = (key: string, options?: object): string => {
  return i18n.t(key, options);
};

export default translate;

// 带命名空间前缀的 Hook
export const useTranslationWithPrefix = (prefix: string) => {
  const { t: originalT } = useTranslation();
  
  const t = (key: string, options?: object) => {
    return originalT(`${prefix}:${key}`, options);
  };
  
  return { t };
};
```

**使用示例**:
```javascript
// 基础用法
import t from 'translations/translate';
const title = t('button.send');  // "发送"

// 带命名空间
import { useTranslationWithPrefix } from 'translations/translate';
const { t } = useTranslationWithPrefix('auth');
const title = t('title.welcomeToPillar');  // "欢迎来到支柱"

// 带插值变量
import t from 'translations/translate';
const greeting = t('greeting', { username: '张三' });  // "欢迎，张三！"
```

**路径**: `src/translations/translate.tsx`

---

### 6.2 `src/utils/cache.js`

**功能**: 翻译资源缓存管理

**关键函数**:
```javascript
export const getCachedTranslationResources = (
  language: string,
  cachedUrls: Object
): ?TranslationResourcesOfLanguage => {
  // 从缓存中获取翻译资源
};
```

**路径**: `src/utils/cache.js`

---

## 7️⃣ 文件关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户界面层 (UI Layer)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐      ┌──────────────────────┐        │
│  │  SelectLanguage.js   │ ───► │ LanguageSetting.js   │        │
│  │  (语言选择页面)       │      │ (设置入口)            │        │
│  └──────────────────────┘      └──────────────────────┘        │
│           │                              │                      │
│           │                              │                      │
└───────────┼──────────────────────────────┼──────────────────────┘
            │                              │
            ▼                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Action 层 (Actions)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────┐       │
│  │         localisationActions.js                      │       │
│  │  ┌─────────────────────────────────────────────┐    │       │
│  │  │  changeLanguageAction(language)             │    │       │
│  │  │    1. 检查语言是否支持                       │    │       │
│  │  │    2. 加载翻译资源                           │    │       │
│  │  │    3. 调用 setLanguage()                    │    │       │
│  │  │    4. 调用 setAppLanguageAction()           │    │       │
│  │  └─────────────────────────────────────────────┘    │       │
│  └─────────────────────────────────────────────────────┘       │
│           │                                                     │
│           │                                                     │
│           ├────────────┬────────────┬─────────────┐            │
│           ▼            ▼            ▼             ▼            │
│  ┌──────────────┐ ┌─────────┐ ┌─────────┐ ┌────────────┐     │
│  │ translations │ │ session │ │appSettings│ │   Toast    │     │
│  │   Service    │ │ Actions │ │ Actions  │ │  (提示)    │     │
│  └──────────────┘ └─────────┘ └─────────┘ └────────────┘     │
└─────────────────────────────────────────────────────────────────┘
            │            │            │
            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Reducer 层 (State Management)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐      ┌──────────────────────┐        │
│  │ appSettingsReducer   │      │  sessionReducer      │        │
│  │  ┌────────────────┐  │      │  ┌────────────────┐ │        │
│  │  │ localisation:  │  │      │  │ languageCode:  │ │        │
│  │  │  activeLngCode │  │      │  │ version:       │ │        │
│  │  └────────────────┘  │      │  └────────────────┘ │        │
│  └──────────────────────┘      └──────────────────────┘        │
│           │                              │                      │
└───────────┼──────────────────────────────┼──────────────────────┘
            │                              │
            ▼                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    服务层 (Service Layer)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────────────────────────────────────────┐        │
│  │      services/localisation/translations.tsx        │        │
│  │                                                     │        │
│  │  ┌──────────────────────────────────────────┐     │        │
│  │  │  i18next 核心实例                         │     │        │
│  │  │  - initTranslations()                    │     │        │
│  │  │  - setLanguage(lng, resources)           │     │        │
│  │  │  - isLanguageSupported(lng)              │     │        │
│  │  │  - getDefaultSupportedUserLanguage()     │     │        │
│  │  └──────────────────────────────────────────┘     │        │
│  └────────────────────────────────────────────────────┘        │
│           │                                                     │
│           │                                                     │
└───────────┼─────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   配置 & 资源层 (Config & Resources)             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐      │
│  │            configs/localeConfig.js                   │      │
│  │  ┌────────────────────────────────────────────┐     │      │
│  │  │  supportedLanguages: {                     │     │      │
│  │  │    en: 'English',                          │     │      │
│  │  │    cn: 'Chinese (Simplified)',             │     │      │
│  │  │    ...                                     │     │      │
│  │  │  }                                         │     │      │
│  │  │  localTranslations: {                      │     │      │
│  │  │    cn: { common: CN_COMMON, auth: ... }   │     │      │
│  │  │  }                                         │     │      │
│  │  └────────────────────────────────────────────┘     │      │
│  └──────────────────────────────────────────────────────┘      │
│           │                                                     │
│           ▼                                                     │
│  ┌──────────────────────────────────────────────────────┐      │
│  │         locales/ (翻译资源文件)                       │      │
│  │  ┌────────────────────────────────────────────┐     │      │
│  │  │  zh-CN/                                    │     │      │
│  │  │    ├── common.json (1602 行)               │     │      │
│  │  │    └── auth.json                           │     │      │
│  │  │                                            │     │      │
│  │  │  en/                                       │     │      │
│  │  │    ├── common.json (1779 行) ✓             │     │      │
│  │  │    └── auth.json                           │     │      │
│  │  └────────────────────────────────────────────┘     │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8️⃣ 快速定位指南

### 🔍 按功能定位

#### 想要修改中文翻译文本
```
📍 位置: src/locales/zh-CN/common.json
📍 位置: src/locales/zh-CN/auth.json
```

#### 想要添加新的支持语言
```
📍 配置: src/configs/localeConfig.js
📍 翻译: 新建 src/locales/{语言代码}/ 目录
📍 UI: src/screens/Menu/SelectLanguage.js (添加语言选项)
```

#### 想要修改语言切换逻辑
```
📍 Action: src/actions/localisationActions.js
📍 Service: src/services/localisation/translations.tsx
```

#### 想要修改语言选择界面
```
📍 页面: src/screens/Menu/SelectLanguage.js
📍 入口: src/screens/Menu/Settings/components/LanguageSetting.js
```

#### 想要获取当前语言
```
📍 Selector: src/selectors/appSettings.js (languageSelector)
📍 Hook: useRootSelector(languageSelector)
```

#### 想要在代码中使用翻译
```
📍 工具: src/translations/translate.tsx

// 基础用法
import t from 'translations/translate';
const text = t('button.send');

// 带命名空间
import { useTranslationWithPrefix } from 'translations/translate';
const { t } = useTranslationWithPrefix('auth');
const text = t('title.welcome');
```

---

### 🔍 按文件类型定位

#### 📝 配置文件
| 文件 | 功能 | 路径 |
|------|------|------|
| `localeConfig.js` | 核心配置 | `src/configs/localeConfig.js` |
| `localesConstants.js` | 常量定义 | `src/constants/localesConstants.js` |

#### 📚 翻译资源
| 文件 | 语言 | 行数 | 路径 |
|------|------|------|------|
| `common.json` | 中文 | 1602 | `src/locales/zh-CN/common.json` |
| `auth.json` | 中文 | - | `src/locales/zh-CN/auth.json` |
| `common.json` | 英文 | 1779 | `src/locales/en/common.json` |
| `auth.json` | 英文 | - | `src/locales/en/auth.json` |

#### 🎨 UI 组件
| 文件 | 功能 | 路径 |
|------|------|------|
| `SelectLanguage.js` | 语言选择页面 | `src/screens/Menu/SelectLanguage.js` |
| `LanguageSetting.js` | 设置入口组件 | `src/screens/Menu/Settings/components/LanguageSetting.js` |

#### ⚡ Actions
| 文件 | 功能 | 路径 |
|------|------|------|
| `localisationActions.js` | 语言切换核心 | `src/actions/localisationActions.js` |
| `appSettingsActions.js` | 应用设置 | `src/actions/appSettingsActions.js` |
| `sessionActions.js` | 会话管理 | `src/actions/sessionActions.js` |

#### 🗄️ Reducers
| 文件 | 功能 | 路径 |
|------|------|------|
| `appSettingsReducer.js` | 应用设置状态 | `src/reducers/appSettingsReducer.js` |
| `sessionReducer.js` | 会话状态 | `src/reducers/sessionReducer.js` |

#### 🔧 Services
| 文件 | 功能 | 路径 |
|------|------|------|
| `translations.tsx` | i18next 核心服务 | `src/services/localisation/translations.tsx` |
| `postProcessors.js` | 文本后处理器 | `src/services/localisation/postProcessors.js` |

#### 🛠️ Utils
| 文件 | 功能 | 路径 |
|------|------|------|
| `translate.tsx` | 翻译函数封装 | `src/translations/translate.tsx` |
| `cache.js` | 翻译缓存管理 | `src/utils/cache.js` |

---

## 9️⃣ 常见操作示例

### 示例 1: 修改欢迎文本

**文件**: `src/locales/zh-CN/auth.json`

```json
{
  "title": {
    "welcomeToPillar": "欢迎来到 PillarX",  // 修改这里
    "welcomeToPillarGetStarted": "欢迎来到 PillarX。开始使用"
  }
}
```

---

### 示例 2: 添加新的翻译键

**文件**: `src/locales/zh-CN/common.json`

```json
{
  "button": {
    "send": "发送",
    "receive": "接收",
    "swap": "交换",
    "newButton": "新按钮"  // ← 添加新键
  }
}
```

**使用**:
```javascript
import t from 'translations/translate';

const buttonText = t('button.newButton');  // "新按钮"
```

---

### 示例 3: 启用语言切换功能

**文件**: `src/screens/Menu/Settings/Settings.js`

```javascript
// 取消注释以下行
import LanguageSetting from './components/LanguageSetting';

// ...

<Header>{t('appSettings')}</Header>
<LanguageSetting />  // ← 取消注释
<BiometricLoginSetting />
```

---

### 示例 4: 修改语言显示名称

**文件**: `src/configs/localeConfig.js`

```javascript
supportedLanguages: {
  [DEFAULT_LANGUAGE_CODE]: DEFAULT_LANGUAGE,
  am: 'አማርኛ',
  bs: 'Bosanski',
  cn: '简体中文',  // ← 改为中文显示
}
```

---

## 🔟 国际化最佳实践

### ✅ 推荐做法

1. **始终使用翻译函数**
   ```javascript
   // ✅ 正确
   <Button title={t('button.send')} />
   
   // ❌ 错误
   <Button title="Send" />
   ```

2. **使用命名空间分组**
   ```javascript
   const { t } = useTranslationWithPrefix('auth');
   const title = t('title.welcome');  // auth:title.welcome
   ```

3. **保持键名语义化**
   ```json
   {
     "button": {
       "send": "发送",
       "cancel": "取消"
     },
     "error": {
       "networkError": "网络错误"
     }
   }
   ```

4. **使用插值变量**
   ```json
   {
     "greeting": "欢迎，{{username}}！"
   }
   ```
   ```javascript
   t('greeting', { username: '张三' })  // "欢迎，张三！"
   ```

---

### ⚠️ 避免的做法

1. **硬编码文本**
   ```javascript
   // ❌ 不要这样做
   const title = "发送";
   ```

2. **禁用国际化检查**
   ```javascript
   // ❌ 避免使用
   /* eslint-disable i18next/no-literal-string */
   ```

3. **翻译键重复或冗长**
   ```json
   // ❌ 不好的结构
   {
     "sendButton": "发送",
     "sendButtonText": "发送",
     "buttonSend": "发送"
   }
   ```

---

## 1️⃣1️⃣ 已知问题和改进建议

### 🐛 已知问题

1. **翻译不完整**
   - ❌ 中文翻译文件比英文少 177 行（约 10%）
   - 📍 位置: `src/locales/zh-CN/common.json`

2. **机器翻译质量问题**
   - ❌ "tutorialTitle": "欢迎来到柱子！"（应为"欢迎来到 Pillar"）
   - ❌ Lorem Ipsum 被错误翻译

3. **大量硬编码英文**
   - ❌ 4000+ 处硬编码字符串
   - ❌ 639 个文件包含未翻译文本

4. **语言切换功能被禁用**
   - ⚠️ `LanguageSetting` 组件在设置页面被注释
   - 📍 位置: `src/screens/Menu/Settings/Settings.js`

---

### 💡 改进建议

#### 短期 (1-2 周)

1. **补全缺失翻译**
   ```bash
   # 对比英文和中文文件，找出缺失的 177 行
   需要人工翻译和补充
   ```

2. **修正机器翻译错误**
   - 专有名词保持英文或音译
   - Lorem Ipsum 等占位符不翻译

3. **启用语言切换功能**
   ```javascript
   // src/screens/Menu/Settings/Settings.js
   // 取消注释 LanguageSetting 组件
   ```

4. **改善语言显示名称**
   ```javascript
   // src/configs/localeConfig.js
   cn: '简体中文'  // 改为中文
   ```

---

#### 中期 (1-2 月)

1. **重构硬编码字符串**
   - 使用工具扫描硬编码文本
   - 逐步替换为 `t()` 函数调用

2. **建立翻译流程**
   - 新功能必须同步添加中英文
   - CI/CD 检查翻译完整性

3. **统一术语翻译**
   - 建立术语表
   - 保持专业术语一致性

---

#### 长期 (3-6 月)

1. **移除 ESLint 禁用规则**
   ```javascript
   // 移除所有文件中的
   /* eslint-disable i18next/no-literal-string */
   ```

2. **实现动态翻译加载**
   - 按需加载翻译资源
   - 减少初始包大小

3. **支持更多语言**
   - 繁体中文
   - 日语、韩语等

---

## 1️⃣2️⃣ 调试和测试

### 🔍 如何测试语言切换

1. **通过 UI 测试**
   ```
   1. 打开应用
   2. 进入 菜单 → 设置 → 语言
   3. 选择“简体中文”
   4. 检查界面是否切换为中文
   
   ⚠️ 注意：需要先启用 LanguageSetting 组件（取消注释）
   ```

2. **通过代码测试**
   ```javascript
   import { useDispatch } from 'react-redux';
   import { changeLanguageAction } from 'actions/localisationActions';
   
   const TestLanguageSwitch = () => {
     const dispatch = useDispatch();
     
     const switchLanguage = () => {
       // 切换到中文
       dispatch(changeLanguageAction('cn'));
       
       // 或者切换到英文
       // dispatch(changeLanguageAction('en'));
     };
     
     return <Button onPress={switchLanguage} title="切换语言" />;
   };
   ```

3. **检查当前语言**
   ```javascript
   import { useRootSelector } from 'react-redux';
   import { languageSelector } from 'selectors/appSettings';
   
   const LanguageDebug = () => {
     const currentLanguage = useRootSelector(languageSelector);
     
     console.log('当前语言:', currentLanguage);  // 'cn' 或 'en'
     
     return (
       <View>
         <Text>当前语言: {currentLanguage}</Text>
         <Text>是否为中文: {currentLanguage === 'cn' ? '是' : '否'}</Text>
       </View>
     );
   };
   ```

4. **检查设备默认语言**
   ```javascript
   import { getLocales } from 'react-native-localize';
   import { getDefaultSupportedUserLanguage } from 'services/localisation/translations';
   
   // 获取设备所有语言
   const deviceLocales = getLocales();
   console.log('设备语言列表:', deviceLocales);
   // 输出: [{ languageCode: 'zh', countryCode: 'CN', ... }, ...]
   
   // 获取默认支持的语言
   const defaultLang = getDefaultSupportedUserLanguage();
   console.log('默认语言:', defaultLang);  // 'cn' 或 'en'
   ```

---

### 🛠️ 调试技巧

1. **查看加载的翻译资源**
   ```javascript
   import i18n from 'i18next';
   
   console.log('支持的语言:', i18n.languages);
   console.log('当前语言:', i18n.language);
   console.log('翻译资源:', i18n.store.data);
   ```

2. **测试特定翻译键**
   ```javascript
   import t from 'translations/translate';
   
   console.log(t('button.send'));  // 应输出: "发送"
   ```

3. **检查缺失翻译**
   ```javascript
   // 如果翻译键不存在，会返回键名本身
   console.log(t('nonexistent.key'));  // 输出: "nonexistent.key"
   ```

---

## 1️⃣3️⃣ 参考资源

### 📚 官方文档

- **i18next**: https://www.i18next.com/
- **react-i18next**: https://react.i18next.com/
- **React Native Localization**: https://reactnative.dev/docs/localization

### 🔗 相关文件链接

| 类型 | 文件路径 |
|------|---------|
| 配置 | `src/configs/localeConfig.js` |
| 中文翻译 | `src/locales/zh-CN/common.json` |
| 语言切换 | `src/actions/localisationActions.js` |
| i18next 服务 | `src/services/localisation/translations.tsx` |
| 翻译工具 | `src/translations/translate.tsx` |

---

## 1️⃣4️⃣ 总结

### 核心文件清单

**必须了解的 5 个文件**:

1. ✅ `src/configs/localeConfig.js` - 语言配置
2. ✅ `src/locales/zh-CN/common.json` - 中文翻译
3. ✅ `src/actions/localisationActions.js` - 语言切换逻辑
4. ✅ `src/services/localisation/translations.tsx` - i18next 核心
5. ✅ `src/translations/translate.tsx` - 翻译工具函数

---

### 当前状态评估

| 维度 | 评分 | 说明 |
|------|------|------|
| **架构设计** | ⭐⭐⭐⭐☆ | 使用 i18next，架构清晰 |
| **配置完整性** | ⭐⭐⭐⭐☆ | 配置文件齐全 |
| **翻译完整度** | ⭐⭐⭐☆☆ | 缺失 10% 内容 |
| **翻译质量** | ⭐⭐☆☆☆ | 机器翻译未校对 |
| **代码规范** | ⭐⭐☆☆☆ | 大量硬编码 |
| **用户体验** | ⭐⭐☆☆☆ | 中英文混杂 |

---

### 快速改进清单

- [ ] 补全缺失的 177 行中文翻译
- [ ] 修正机器翻译错误（专有名词、Lorem Ipsum）
- [ ] 启用语言切换功能（取消注释 LanguageSetting）
- [ ] 改善语言显示名称（cn: '简体中文'）
- [ ] 扫描并重构前 100 个高频硬编码字符串
- [ ] 建立翻译更新流程

---

**文档版本**: v2.0 (精确版)  
**最后更新**: 2024-11-27  
**维护者**: Cascade AI Assistant

---

## 1️⃣4️⃣ 技术栈说明

### ✅ 实际使用的技术栈

**核心框架**:
- ✅ **i18next v23.4.5** - 国际化核心引擎
- ✅ **react-i18next v15.0.6** - React Native 集成
- ✅ **react-native-localize v3.5.1** - 设备语言检测
- ✅ **Redux Toolkit v2.8.2** - 状态管理

**关键特性**:
- ✅ 支持多语言：`en`, `am`, `bs`, `cn`
- ✅ 命名空间分离：`common`, `auth`
- ✅ 后处理器：首字母大写、标点符号、前后缀
- ✅ 热更新：支持运行时切换语言
- ✅ 缓存机制：支持 Firebase Remote Config 远程翻译
- ✅ 回退机制：自动回退到英文

**与 v1.0 文档的主要差异**:

| 项目 | v1.0 文档描述 | v2.0 实际代码 | 状态 |
|------|---------------|--------------|------|
| 设备语言获取 | `Localization.locale` | `getLocales()` from `react-native-localize` | ✅ 已修正 |
| `setLanguage` 签名 | 4 个参数 | 1 个参数 (`lng`) | ✅ 已修正 |
| 后处理器命名 | camelCase | PascalCase | ✅ 已修正 |
| `changeLanguageAction` | 简化版 | 完整实现版 | ✅ 已修正 |
| Toast emoji | `checkmark` | `ok_hand` | ✅ 已修正 |

---

📝 **使用建议**: 
- 将此文档加入项目 README 或 Wiki
- 定期更新翻译完成度
- 建立翻译质量检查流程

🔗 **相关文档**: 
- 国际化规范: `src/services/localisation/README.md`
- 项目配置: `README.md`
